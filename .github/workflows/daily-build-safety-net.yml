name: Daily Build Safety Net

# Runs 10 minutes after Netlify's 09:30 UTC scheduled build. If daily:report:{today}
# already exists in Redis with cards, Netlify's scheduler worked — stand down, no
# duplicate build, no double email. If it's missing, trigger the unscheduled test
# twin (daily-build-test-background.js) as a fallback so cards still land by ~10:45
# Irish time. This does NOT touch the heartbeat tripwires in daily-build-background.js
# — those keep recording whether Netlify's own scheduler actually fired, so this net
# adds a fallback without hiding the underlying evidence.
#
# Also checks build:lock:{today} (AUDIT-DAILY-INTELLIGENCE.md, finding C2): if a
# build is already mid-flight — Netlify's scheduled run just happens to be a slow
# one — the report may not exist yet even though nothing has actually failed.
# Standing down on a fresh lock avoids firing a second, redundant build on top of
# a still-running one.

on:
  schedule:
    - cron: '40 9 * * *'
  workflow_dispatch:
    inputs:
      force:
        description: 'Force a fresh build even if daily:report/intelligence/lock already show one for today (use when today''s report is a stale/partial leftover, e.g. from a mid-run API failure)'
        type: boolean
        default: false
      diagnose:
        description: 'Print today''s raw daily:report, build:lock and build:crash from Redis (errors, startedAt/completedAt, lock age) without triggering anything'
        type: boolean
        default: false

jobs:
  safety-net:
    runs-on: ubuntu-latest
    steps:
      - name: Check whether today's report already exists or a build is in progress
        id: check
        env:
          UPSTASH_URL: ${{ secrets.UPSTASH_REDIS_REST_URL }}
          UPSTASH_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}
        run: |
          set -euo pipefail
          TODAY=$(date -u +%Y-%m-%d)
          echo "Checking daily:report:$TODAY and build:lock:$TODAY"

          fetch_redis() {
            local key="$1"
            local out status
            out=$(curl -s -w '\n%{http_code}' -H "Authorization: Bearer $UPSTASH_TOKEN" "$UPSTASH_URL/get/$key")
            status=$(echo "$out" | tail -n1)
            if [ "$status" != "200" ]; then
              echo "::error::Upstash Redis check failed for key $key (HTTP $status) — cannot verify today's build state. Refusing to silently stand down or silently skip the fallback."
              exit 1
            fi
            echo "$out" | sed '$d'
          }

          REPORT_RESPONSE=$(fetch_redis "daily%3Areport%3A$TODAY")
          INTEL_RESPONSE=$(fetch_redis "intelligence%3A$TODAY")
          LOCK_RESPONSE=$(fetch_redis "build%3Alock%3A$TODAY")

          # Success signal from daily:report:{today}: a build proves it ran via
          # report.analyses (NAP/NB race analyses, checkpointed batch by batch) or
          # report.completedAt (set at the very end of every finished build). The
          # old check counted report.intelligence items — permanently 0 since Daily
          # Intelligence generation was paused (commit 569e15d), which left the
          # 30-minute lock as the only stand-down signal and would fire a duplicate
          # build + duplicate email any day the real build finished in under 10
          # minutes. startedAt must still be today so the minimal tomorrow-preview
          # shape (no startedAt/analyses/completedAt) can never stand the net down.
          ITEMS=$(TODAY="$TODAY" node -e '
            let data = "";
            process.stdin.on("data", c => data += c);
            process.stdin.on("end", () => {
              try {
                const raw = JSON.parse(data);
                if (!raw.result) { console.log(0); return; }
                const report = JSON.parse(raw.result);
                const startDate = (report.startedAt || "").slice(0, 10);
                if (startDate !== process.env.TODAY) { console.log(0); return; }
                const analyses = Array.isArray(report.analyses) ? report.analyses.length : 0;
                if (analyses > 0 || report.completedAt) { console.log(Math.max(analyses, 1)); return; }
                console.log(0);
              } catch (e) { console.log(0); }
            });
          ' <<< "$REPORT_RESPONSE")

          # Second, independent signal: intelligence:{today} is written directly by a
          # real build whenever it generates non-empty cards for today. Checking it
          # separately from daily:report:{today} is what actually prevents the
          # duplicate-email case — Netlify's 09:30 build can finish and clear its
          # lock well before this net's own 09:40 check runs, and daily:report's own
          # shape has already changed once (generatedAt -> startedAt); a second,
          # independently-sourced signal survives a future reshape of either key.
          INTEL_ITEMS=$(node -e '
            let data = "";
            process.stdin.on("data", c => data += c);
            process.stdin.on("end", () => {
              try {
                const raw = JSON.parse(data);
                if (!raw.result) { console.log(0); return; }
                const intel = JSON.parse(raw.result);
                const items = Array.isArray(intel.items) ? intel.items : [];
                const n = items.filter(function(it) { return it && it.isTomorrow !== true; }).length;
                console.log(n);
              } catch (e) { console.log(0); }
            });
          ' <<< "$INTEL_RESPONSE")

          LOCK_ACTIVE=$(node -e '
            let data = "";
            process.stdin.on("data", c => data += c);
            process.stdin.on("end", () => {
              try {
                const raw = JSON.parse(data);
                if (!raw.result) { console.log("false"); return; }
                const lock = JSON.parse(raw.result);
                if (!lock || !lock.startedAt) { console.log("false"); return; }
                const ageMs = Date.now() - new Date(lock.startedAt).getTime();
                console.log(ageMs >= 0 && ageMs < 30 * 60 * 1000 ? "true" : "false");
              } catch (e) { console.log("false"); }
            });
          ' <<< "$LOCK_RESPONSE")

          echo "today=$TODAY" >> "$GITHUB_OUTPUT"
          echo "items=$ITEMS" >> "$GITHUB_OUTPUT"
          echo "intelItems=$INTEL_ITEMS" >> "$GITHUB_OUTPUT"
          echo "lockActive=$LOCK_ACTIVE" >> "$GITHUB_OUTPUT"
          if [ "${{ github.event.inputs.force }}" = "true" ]; then
            echo "standDown=false" >> "$GITHUB_OUTPUT"
          elif [ "$ITEMS" -gt 0 ] || [ "$INTEL_ITEMS" -gt 0 ] || [ "$LOCK_ACTIVE" = "true" ]; then
            echo "standDown=true" >> "$GITHUB_OUTPUT"
          else
            echo "standDown=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Stand down — report already exists or a build is in progress
        if: steps.check.outputs.standDown == 'true'
        run: |
          if [ "${{ steps.check.outputs.items }}" -gt 0 ]; then
            echo "daily:report:${{ steps.check.outputs.today }} shows a completed/in-progress build (${{ steps.check.outputs.items }} race analysis/analyses). Netlify's scheduled build ran — nothing to do."
          elif [ "${{ steps.check.outputs.intelItems }}" -gt 0 ]; then
            echo "intelligence:${{ steps.check.outputs.today }} already has ${{ steps.check.outputs.intelItems }} card(s). Netlify's scheduled build ran — nothing to do."
          else
            echo "build:lock:${{ steps.check.outputs.today }} shows a build already in progress — standing down to avoid a duplicate."
          fi

      - name: Note forced override
        if: github.event.inputs.force == 'true'
        run: |
          echo "::warning::force=true — bypassing the stand-down check regardless of today's report/intelligence/lock state (items=${{ steps.check.outputs.items }}, intelItems=${{ steps.check.outputs.intelItems }}, lockActive=${{ steps.check.outputs.lockActive }})."

      - name: Diagnose — dump today's raw report/lock/crash state
        if: github.event.inputs.diagnose == 'true'
        env:
          UPSTASH_URL: ${{ secrets.UPSTASH_REDIS_REST_URL }}
          UPSTASH_TOKEN: ${{ secrets.UPSTASH_REDIS_REST_TOKEN }}
        run: |
          set -euo pipefail
          TODAY=$(date -u +%Y-%m-%d)
          fetch_redis() {
            curl -s -H "Authorization: Bearer $UPSTASH_TOKEN" "$UPSTASH_URL/get/$1"
          }
          echo "== build:lock:$TODAY =="
          fetch_redis "build%3Alock%3A$TODAY" | node -e 'let d="";process.stdin.on("data",c=>d+=c);process.stdin.on("end",()=>{try{const r=JSON.parse(d);console.log(r.result?JSON.stringify(JSON.parse(r.result),null,2):"(absent)");}catch(e){console.log("(unparseable) "+d.slice(0,300));}})'
          echo
          echo "== build:crash:$TODAY =="
          fetch_redis "build%3Acrash%3A$TODAY" | node -e 'let d="";process.stdin.on("data",c=>d+=c);process.stdin.on("end",()=>{try{const r=JSON.parse(d);console.log(r.result?JSON.stringify(JSON.parse(r.result),null,2):"(absent)");}catch(e){console.log("(unparseable) "+d.slice(0,300));}})'
          echo
          echo "== debug:build-heartbeat:$TODAY =="
          fetch_redis "debug%3Abuild-heartbeat%3A$TODAY" | node -e 'let d="";process.stdin.on("data",c=>d+=c);process.stdin.on("end",()=>{try{const r=JSON.parse(d);console.log(r.result?JSON.stringify(JSON.parse(r.result),null,2):"(absent)");}catch(e){console.log("(unparseable) "+d.slice(0,300));}})'
          echo
          echo "== daily:report:$TODAY (summary fields only) =="
          fetch_redis "daily%3Areport%3A$TODAY" | node -e 'let d="";process.stdin.on("data",c=>d+=c);process.stdin.on("end",()=>{try{const r=JSON.parse(d);if(!r.result){console.log("(absent)");return;}const rep=JSON.parse(r.result);console.log(JSON.stringify({startedAt:rep.startedAt,completedAt:rep.completedAt,meetings:rep.meetings,totalRaces:rep.totalRaces,upcomingRaces:rep.upcomingRaces,racesAnalysed:rep.racesAnalysed,analysesLength:(rep.analyses||[]).length,intelligenceItems:rep.intelligenceItems,costUSD:rep.costUSD,errors:rep.errors,warnings:rep.warnings},null,2));}catch(e){console.log("(unparseable) "+d.slice(0,300));}})'

      - name: Trigger fallback build — no report found and no build in progress
        if: steps.check.outputs.standDown != 'true'
        env:
          BUILD_SECRET: ${{ secrets.BUILD_SECRET }}
        run: |
          set -euo pipefail
          echo "No report found for ${{ steps.check.outputs.today }} — Netlify's 09:30 UTC scheduler appears to have failed. Triggering the fallback build."

          HTTP_STATUS=$(curl -s -o /tmp/fallback_response.json -w "%{http_code}" -X POST \
            -H "x-build-secret: $BUILD_SECRET" \
            "https://superlative-flan-93dfc4.netlify.app/.netlify/functions/daily-build-test-background")

          echo "Fallback build trigger returned HTTP $HTTP_STATUS"
          cat /tmp/fallback_response.json || true

          if [ "$HTTP_STATUS" != "202" ]; then
            echo "::error::Fallback build trigger failed with HTTP $HTTP_STATUS"
            exit 1
          fi

          echo "::warning::Netlify's scheduled build did not run today — fallback triggered via GitHub Actions. Cards should land within ~15 minutes."
